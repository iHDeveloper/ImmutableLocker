function hashPassphrase(passphrase) {
    // TODO hash the passphrase using SHA-256
}

function checksum(data) {
    // TODO checksum the data using SHA-512
}

function generateSalt() {
    // TODO generate a 64bit random salt using crypto API
}

function generateKey(hash, salt) {
    // TODO encrypt the hash with salt using scrypt
}

function ecnrypt(key, data) {
    // TODO encode the data within private key using AES-256
}

function encode(encryptedData) {
    // TODO encode encrypted data to Base64
}
